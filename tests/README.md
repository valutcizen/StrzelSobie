# Tests

This directory contains automated tests for the project. The structure mirrors the `src` directory, with a corresponding test folder for each backend module.

Contract test templates for each module live alongside the folders and can be expanded as implementations of the shared interfaces in `@strzel-sobie/common` evolve.
