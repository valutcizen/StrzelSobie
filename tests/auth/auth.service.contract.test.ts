import { describe, it } from 'vitest';
import type { IAuthService } from '@strzel-sobie/common/src/auth/service';

type ContractSubject = IAuthService;

describe('AuthService contract', () => {
  it.todo('validates that the @strzel-sobie/auth implementation satisfies IAuthService');
});
