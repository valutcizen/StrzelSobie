import { describe, it } from 'vitest';
import type { IRangesService } from '@strzel-sobie/common/src/ranges/service';

type ContractSubject = IRangesService;

describe('RangesService contract', () => {
  it.todo('validates that the @strzel-sobie/ranges implementation satisfies IRangesService');
});
