import { describe, it } from 'vitest';

describe('NotificationsService contract', () => {
  it.todo('validates the future @strzel-sobie/notifications implementation once the common contract exists');
});
