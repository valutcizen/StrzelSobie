import { describe, it } from 'vitest';
import type { IReservationsService } from '@strzel-sobie/common/src/reservations/service';

type ContractSubject = IReservationsService;

describe('ReservationsService contract', () => {
  it.todo('validates that the @strzel-sobie/reservations implementation satisfies IReservationsService');
});
