{
  "name": "strzel-sobie",
  "private": true,
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
