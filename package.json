{
  "name": "strzel-sobie",
  "private": true,
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build -w src/common && npm run build -w src/auth -w src/users -w src/reservations -w src/admin -w src/notifications && npm run build -w src/worker && npm run build -w src/client",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  }
}
