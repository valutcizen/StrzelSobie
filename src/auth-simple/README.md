# Auth Simple Module

This module provides a simple, hardcoded auth backend for local testing and demos. It uses a fixed list of users and plaintext passwords, creates sessions via the shared session repository, and blocks registration with a 403 error.
