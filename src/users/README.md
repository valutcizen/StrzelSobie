# Users Module

This module manages user accounts, profiles, roles, and the verification process for Members and Coordinators.

It also owns administrator contact profile data (global defaults and per-range overrides), including visibility flags used by range detail projections.
